<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Uploading</title>
</head>

<body>
<h1>To Upload Image on mongoDB</h1>
<hr>
<div>
    <form action="/" method="POST" enctype="multipart/form-data">
        <div>
            <label for="name">Image Title</label>
            <input type="text" id="name" placeholder="Name"
                   value="" name="name" required>
        </div>
        <div>
            <label for="desc">Image Description</label>
            <textarea id="desc" name="desc" value="" rows="2"
                      placeholder="Description" required>
                </textarea>
        </div>
        <div>
            <label for="currentToken">Current Token</label>
            <input type="text" id="currentToken" placeholder="Token"
                   value="" name="currentToken" required>
        </div>
        <div>
            <label for="metadata">Metadata</label>
            <input type="text" id="metadata" placeholder="Data"
                   value="" name="metadata" required>
        </div>
        <div>
            <label for="width">Width of the picture</label>
            <input type="text" id="width" placeholder="0"
                   value="" name="width" required>
        </div>
        <div>
            <label for="height">Height of the picture</label>
            <input type="text" id="height" placeholder="0"
                   value="" name="height" required>
        </div>
        <div>
            <label for="pixels">Number of pixels</label>
            <input type="text" id="pixels" placeholder="0"
                   value="" name="pixels" required>
        </div>
        <div>
            <label for="image">Upload Image</label>
            <input type="file" id="image"
                   name="image" value="" required>
        </div>
        <div>
            <button type="submit">Submit</button>
        </div>
    </form>
</div>

<hr>

<h1>Uploaded Images</h1>
<div>
    <% items.forEach(function(image) { %>
    <div>
        <div>
            <img src="data:image/<%=image.img.contentType%>;base64,
                     <%=image.img.data.toString('base64')%>">
            <div>
                <h5><%= image.name %></h5>



                <p><%= image.desc %></p>



            </div>
        </div>
    </div>
    <% }) %>
</div>
</body>

</html>